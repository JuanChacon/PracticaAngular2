import { Pipe, PipeTransform } from "@angular/core";

@Pipe({name: "prueba"})

export class pruebasPipe {
//implements PipeTransform{
   /*
    transform(value:number, [por]) : string{
        let porv = parseInt(por);
        let valuev = parseInt(value);

        let resul = "la multiplicacion:  " + valuev+ "x" +porv + ":" + "=" + (value*porv); 
        return resul;
    }
    */
}

